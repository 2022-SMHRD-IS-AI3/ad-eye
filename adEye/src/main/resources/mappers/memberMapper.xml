<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sansam.adeye.mapper.MemberMapper">
  
  	<update id="update" parameterType="MemberDTO">
		UPDATE MEMBER SET mem_pw = #{pw}, mem_phone = #{phone}, mem_email=#{email}, company_addr = #{addr} where mem_id = #{id}
	</update>

	<insert id="insert" parameterType="MemberDTO">
		insert into member values(#{id}, #{pw}, #{company}, #{phone}, #{email}, 'Y', sysdate, #{addr}, #{admin})
	</insert>
	
	<select id="login" resultType="MemberDTO" parameterType="MemberDTO">
		SELECT * FROM member WHERE id = #{id} AND pw = #{pw}
	</select>
	
	<update id="delete" parameterType="MemberDTO">
		UPDATE WEB_MEMBER SET mem_state = 'D' where mem_id = #{id}
	</update>
  
</mapper>