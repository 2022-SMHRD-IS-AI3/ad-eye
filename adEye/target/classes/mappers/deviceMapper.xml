<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="DeviceMapper">
  
  	<insert id="create">
		insert into tb_device (device_uid, device_onoff, device_status, device_dt) values(#{device_uid}, #{device_onoff}, #{device_status}, sysdate)
	</insert>
	<select id="list" resultType="com.sansam.adeye.domain.DeviceDTO">
		<![CDATA[select device_seq,
						device_uid,
						device_onoff,
						device_status,
						device_dt
		 	     from (
		 	     	select /*+ INDEX_DESC(tb_device IDX_tb_device_PK) */ 
		 	     		rownum rn
						device_seq,
						device_uid,
						device_onoff,
						device_status,
						device_dt
		       		from tb_device
		           	where
		]]>
		<include refid="criteria"></include>         
      	<![CDATA[
               rownum <= #{pageNum} * #{amount}
              )
         WHERE rn > (#{pageNum} - 1) * #{amount}
      	]]>
	</select>
	<sql id="criteria"/>
	
	<select id="detail" parameterType="com.sansam.adeye.domain.DeviceDTO">
		select * from tb_device where device_seq = #{device_seq}
	</select>
	
	<delete id="delete" parameterType="int">
		delete from tb_device where device_seq = #{device_seq}
	</delete>

	<update id="update" parameterType="com.sansam.adeye.domain.DeviceDTO">
		update tb_device set device_uid=#{device_uid}, device_onoff = 'Y', device_status=#{device_status} where device_seq = #{device_seq}
	</update>
	
	<select id="getOnoff" parameterType="com.sansam.adeye.domain.DeviceDTO">
		select device_seq, device_onoff from tb_device where device_uid = #{device_uid}
	</select>
	
	<update id="control" parameterType="com.sansam.adeye.domain.DeviceDTO">
		update tb_device set device_onoff = #{device_onoff} where device_seq = #{device_seq}
	</update>
	
  
</mapper>