<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ContactMapper">
	
	<!-- <select id="list" resultType="com.sansam.adeye.domain.ContactDTO">
		<![CDATA[
			SELECT	contact_seq,
					email,
					phone,
					company,
					contact_content,
					contact_dt,
					contact_open
			FROM (
				SELECT /*+ INDEX_DESC(tb_contact IDX_tb_contact_PK) */
					rownum rn,
					contact_seq,
					email,
					phone,
					company,
					contact_content,
					contact_dt,
					contact_open
				FROM tb_contact
				WEHRE
		]]>
		<include refid="criteria"></include>
		<![CDATA[
				rownum <= #{pageNum} * #{amount}
            )
         	WHERE rn > (#{pageNum} - 1) * #{amount}
		]]>
	</select> -->
	
	<insert id="create">
		INSERT INTO tb_contact (email, phone, company, contact_content, contact_dt, contact_open, admin_id) values('email_5@sansam.com', '010-0000-0000', '산삼들_7', '우리들은 산삼들_7이다', sysdate, 'N', 'test01')
	</insert>
  
  	<select id="detail" resultType="com.sansam.adeye.domain.ContactDTO" parameterType="int">
		SELECT * from tb_contact WHERE contact_seq = #{contact_seq}
  	</select>
 
</mapper>